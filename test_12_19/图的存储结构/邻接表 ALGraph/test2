#include <stdio.h>
#define MAXV 1005
#define undiredted 1

//边结点结构体
typedef struct ArcNode {
    int to;//邻接顶点的索引
    int w;//边的权值
    struct ArcNode *next;//指向下一个边结点的指针
} ArcNode;

//顶点结构体
typedef struct {
        ArcNode * first;//指向第一条边结点的指针
    }VNode;

//邻接表存储的图结构体
ty
